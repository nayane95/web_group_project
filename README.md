#Run Project

##Commands
`npm install`

`composer install`

`php artisan key:generate`

copy the .env.example file as .env

create db and change it.

`php artisan:migrate`

`npm run serve`

`php artisan:migrtate`
